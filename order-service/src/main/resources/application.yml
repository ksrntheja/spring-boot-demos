# TODO 15: Add order-service properties.
spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  application:
    name: order-service
  datasource:
    url: jdbc:h2:mem:orders;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081

logging:
  level:
    org.hibernate.SQL: DEBUG
